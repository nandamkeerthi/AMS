/** Shared MUI sx overrides for consistent form controls and buttons. */

export const fieldSx = {
  '& .MuiOutlinedInput-root': { borderRadius: '4px' },
};

export const actionButtonSx = {
  borderRadius: '4px',
  textTransform: 'none',
  boxShadow: 'none',
};
